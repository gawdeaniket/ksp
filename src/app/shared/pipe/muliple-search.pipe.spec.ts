import { MulipleSearchPipe } from './muliple-search.pipe';

describe('MulipleSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MulipleSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
