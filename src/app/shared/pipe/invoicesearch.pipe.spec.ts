import { InvoicesearchPipe } from './invoicesearch.pipe';

describe('InvoicesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new InvoicesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
