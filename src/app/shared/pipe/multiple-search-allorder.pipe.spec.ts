import { MultipleSearchAllorderPipe } from './multiple-search-allorder.pipe';

describe('MultipleSearchAllorderPipe', () => {
  it('create an instance', () => {
    const pipe = new MultipleSearchAllorderPipe();
    expect(pipe).toBeTruthy();
  });
});
